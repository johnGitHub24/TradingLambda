# TradingLambda — no Spring Boot application, no bootRun task.
# This is a Java 21 Lambda / Stream lab (JUnit 5 + Gradle only).
#
# IntelliJ on this PC:
#   1. Open project ROOT (not a submodule)
#   2. Project SDK = 21  (C:\Program Files\Java\jdk-21.0.12)
#   3. Gradle Sync; Build and run using = Gradle
#   4. Gradle tool window -> check / test   OR   .\scripts\check.ps1
#
# Do NOT set BOOT_GRADLE_TASK=:order-service:bootRun — that module does not exist.
# Leave BOOT_* unset so fix-intellij-run.ps1 will not invent a fake boot app.

# BOOT_GRADLE_TASK=
# BOOT_RUN_NAME=
# SPRING_BOOT_RUN_NAME=
# SPRING_BOOT_MAIN_CLASS=
# SPRING_BOOT_MODULE=
